<div class="photo-list">
    <ul id="tiles">
        {{#each content}}
        <li {{action 'openModal' 'photoViewer' this}}>
            <img {{bind-attr src="src" }} {{bind-attr alt="name" }}>
            <p>{{name}}</p>
        </li>
        {{/each}} 
    </ul>
</div>
